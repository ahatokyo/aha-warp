# AHA WARP 状態メモ（2026-06-13 時点）

次回CC起動時に続きが分かるための作業メモ。

---

## ⚠️ 最重要：作業クローンの同期について

- 今日の作業はすべて **Temp クローン**（`C:\Users\kazuo\AppData\Local\Temp\aha-warp`）で行い、**origin/main に push 済み**（最新コミット `05a3139`）。
- **`dev` クローン（`C:\Users\kazuo\dev\aha-warp`）は約12コミット遅れている**（HEAD `c5b59a9`）。ローカル変更なし・クリーン。
- 👉 **次回 dev で続ける前に必ず `git pull`（fast-forward で同期）すること。**
- Temp は Windows が消す可能性があるため、**今後の作業は dev に集約推奨**。
- リモート: `https://github.com/ahatokyo/aha-warp.git`（ブランチ main）

---

## ✅ 今日完了したこと（このリポジトリ / git に反映済み）

- **スマホ表示遅延の解消（主因）**：全7ページ（index/login/mypage/pricing/lp/terms/privacy）の背景オーブ `filter:blur(120px)` を `radial-gradient` に置換。これが真犯人で、iOS Safari が初回描画を約6秒停止していた（PCは速い／PETCHAは軽いブラーで速い、で切り分け確定）。commit `7089a21`・`a580c0b`。
- **HERO動画**：
  - 画質復元（1.7Mbps → 5.3Mbps / 10MB / 1080x1920 / 全4シーン15.7秒）commit `d95ec71`
  - 初回描画から切り離し（`opacity:0` → `loadeddata`/`playing` でフェードイン＝PETCHA方式）`33cbe71`
  - `play()` を DOMContentLoaded 非依存の即時実行に `f88a769`
- **HEROテキスト可読性**：オーバーレイを濃く＋小見出し/サブコピーの色・影を強化 `ca1fe2c`
- **ナビのクレジット残数の改行崩れ修正**（`white-space:nowrap`）`05a3139`
- **vercel.json でドメイン出し分け** `3f3beeb`：petcha.aha-tokyo.com=PETCHA / aha-warp.vercel.app=AHA WARP。host条件のredirectで、相手サービスのHTMLにアクセスしたら自分のトップへ。PETCHA本体（petcha.html/api/products/styles/mp4/favicon）は不変。
- **terms.html / privacy.html 配置**、フッターリンク修正、利用規約 第4条に `id="face"`。privacy第3条にOpenAI行追記。
- **mypage.html に顔Asset ID登録の同意フロー**（チェックで保存ボタン解禁、保存時に `face_consent_at` 更新）。
- **favicon誤参照の解消**（※当初「残タスク」と思われていたが今日完了済み）`602bc33`：AWロゴ `favicon.png` から `aha-favicon-32.png`（1.4KB）/ `aha-apple-touch-icon.png`（26KB）を生成し、全AHAページの参照を向け替え。**もうPETCHAのブルドッグfaviconを参照していない**。PETCHA用favicon-32.png等のファイル自体は温存。
- **初期ロード最適化**：favicon肥大化解消、Google Fontsのウェイト削減（Zen Kaku 300/400/700・Syne 700/800）、Supabase SDK の defer化。

## ✅ 今日完了（このリポジトリ外＝git管理外。ユーザー報告ベース）

- **Supabase の RLS / 列権限ロックダウン、RPC の SECURITY DEFINER化**。
  - ※Supabaseダッシュボード側の作業のため git には含まれない。確認するなら本番Supabaseプロジェクト（`majunwobgxvdckyyunxu`）で。

---

## 🔜 残タスク

- **【中期】service role キー共有のリスク**：PETCHA の `api/create-checkout-session.js` と `api/stripe-webhook.js` が `SUPABASE_SERVICE_ROLE_KEY` でクライアント生成しており、**同一Supabaseプロジェクトの全テーブル（AHAの profiles/jobs/reviews 含む）にRLSバイパスで全権アクセスできる**。PETCHA側のバグ/漏洩がAHAデータに波及しうる。
  - 本筋の対策：**PETCHA を別Supabase＋別Vercelプロジェクトに分離**。
  - 短期緩和：各テーブルのRLSを厳格化し、service role使用箇所をPETCHA api内に限定（上記の今日のRLS作業が該当する可能性。要現状確認）。
- **【関連】PETCHA / AHA WARP の完全分離（リポジトリ / Vercel / Supabase）は未着手**。
  - 現状は同一リポジトリ・同一Vercelプロジェクト・同一Supabaseに同居。`vercel.json` でドメイン棲み分けは**暫定対応済み**だが、根本分離はこれから。
  - 安全な分離順序の目安：①ファイル/favicon（完了）→ ②リポジトリ/Vercel分離 → ③env分離 → ④Supabase分離（最難・最後）。

---

## 参考メモ

- 「スマホで数秒真っ白／PCは一瞬」の症状は、まず大きな `filter:blur()` / `backdrop-filter:blur()` を疑う（モバイルGPUで初回描画が止まる）。`radial-gradient` 置換で解決。良い参照実装は PETCHA（petcha.html、最大 blur(12px)）。
- AHAの認証は Supabase Auth（profiles/jobs/reviews）。PETCHAはLINEログイン（pecha_users/pecha_orders/pecha_gifts、line_user_id）。別認証だが**同一Supabaseプロジェクト**に同居。
- AHAの決済は Stripe Payment Link（pricing.html の `buy.stripe.com/...`）。`api/` は使っていない（api/ は100% PETCHA用）。
